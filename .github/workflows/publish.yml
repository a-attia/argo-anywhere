name: Publish to PyPI

# Tag-gated release. Push a version tag (e.g. `v3.0.0`) to build, validate, and
# publish the sdist + wheel to PyPI via Trusted Publishing (OIDC) -- no API
# token or password is stored in the repo. Configure the trusted publisher once
# at https://pypi.org/manage/project/argo-anywhere/settings/publishing/ (or via
# a "pending publisher" before the first upload), matching:
#   owner = a-attia, repo = argo-anywhere, workflow = publish.yml, env = pypi.

on:
  push:
    tags: ["v*"]

permissions:
  contents: read

jobs:
  # Re-run the full test matrix + lint before releasing, so a tag can never
  # publish a red build.
  test:
    name: test (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
      - run: ruff check src tests
      - run: pytest -q

  build:
    name: build + validate
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build sdist + wheel
        run: |
          python -m pip install --upgrade pip build twine
          python -m build
      - name: Verify the tag matches the package version
        run: |
          python - <<'PY'
          import os, pathlib, re
          # version is dynamic from src/argo_anywhere/__init__.py (D-029)
          init = pathlib.Path("src/argo_anywhere/__init__.py").read_text()
          ver = re.search(r'__version__\s*=\s*["\']([^"\']+)["\']', init).group(1)
          tag = os.environ["GITHUB_REF_NAME"]
          assert tag == f"v{ver}", f"tag {tag!r} does not match package version v{ver}"
          print(f"tag {tag} matches version {ver}")
          PY
      - name: Validate metadata (twine check)
        run: python -m twine check dist/*
      - name: Verify the wheel ships the engine + assets
        run: |
          python - <<'PY'
          import glob, zipfile
          whl = sorted(glob.glob("dist/*.whl"))[-1]
          names = zipfile.ZipFile(whl).namelist()
          need = [
              "argo_anywhere/engine/argo-anywhere.sh",
              "argo_anywhere/web/static/index.html",
              "argo_anywhere/web/static/xterm.js",
              "argo_anywhere/assets/icon.icns",
          ]
          missing = [n for n in need if not any(n in x for x in names)]
          assert not missing, f"wheel missing data files: {missing}"
          assert any(x.endswith("licenses/LICENSE") for x in names), "wheel missing LICENSE"
          print("wheel OK:", whl.split("/")[-1])
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/*

  publish:
    name: publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/argo-anywhere
    permissions:
      id-token: write   # OIDC token for Trusted Publishing
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist
      - name: Publish
        uses: pypa/gh-action-pypi-publish@release/v1
